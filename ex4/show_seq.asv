function show_seq(seq)
    [t,y,x] = size(seq);
    
    for num=1 : t
        img = seq(num,:,:);
        size(img)
%         a = a*255;
%         a = uint8(a);
%         imshow(a);
        show_img(seq(num,:,:), [0 1], 'auto', 'image sequence', 256);
        waitforbuttonpress;
    end
end